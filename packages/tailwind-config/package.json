{
  "name": "tailwind-config",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "update": "pnpm update --latest && pnpm install"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
